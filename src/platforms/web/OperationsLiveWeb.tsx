import { OperationsLiveContent } from '../../shared/operations-live/OperationsLiveContent';

/** Web shell — map uses iframe + MapLibre (no react-native-webview). */
export default function OperationsLiveWeb() {
  return <OperationsLiveContent />;
}
